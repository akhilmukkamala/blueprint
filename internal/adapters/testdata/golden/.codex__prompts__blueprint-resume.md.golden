<!-- generated by blueprint v0 for codex; edit nothing here — regenerate with `blueprint sync` -->
# Resume an in-flight blueprint change

Run:

```sh
blueprint resume <change-id> --json
```

The JSON is the resume context: current loop state, last journal events, and remaining tasks.
Continue from the first unchecked task. Never restart finished work; the journal is append-only truth.
