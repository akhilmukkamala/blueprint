---
reviewed:            # YYYY-MM-DD — set when a human curates this file (knowledge lint enforces freshness, max-age 180 days)
owner:               # who answers questions about these terms
status: draft
---

# Glossary

Domain terms as this repo uses them (tribal capture). One row per term; write
the meaning an agent needs to avoid a wrong assumption, not a dictionary entry.

| Term     | Meaning                                                        |
| -------- | -------------------------------------------------------------- |
| _term_   | _the precise in-repo meaning, including what it does NOT mean_ |

Module boundaries live in [architecture](architecture.md).
